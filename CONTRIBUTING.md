# Contributing

Keep parsing deterministic and output stable. New categories or review notes need compact unified-diff fixtures and tests for near-miss paths. Run `go test ./...` before opening a pull request.
